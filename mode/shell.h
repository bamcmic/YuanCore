/*  此文件已迁移到 shell/ 目录（shell/console.c、shell/shell.c 等）。
 *
 *  本文件保留为空壳：当前环境的沙箱不允许我删除项目里的文件，
 *  而文件名与 shell/ 下的同名，若保留旧实现会产生重复符号。
 *  Makefile 仍会编译它，但它不定义任何符号，无副作用。
 *  你可以随手删掉这 4 个文件：mode/shell.c mode/shell.h mode/console.c mode/console.h
 */
typedef int yc_moved_to_shell_dir_t;